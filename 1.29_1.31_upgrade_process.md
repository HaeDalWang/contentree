# Kubernetes 1.29 → 1.31 업그레이드 절차

배포(1.29) 완료 후 업그레이드 테스트용 절차. Kubespray 컨테이너 2.27.1 사용, 변수 파일을 1.30/1.31용으로 준비한 뒤 `upgrade-cluster.yml`을 **CP 먼저, 이후 나머지** 순으로 실행한다.

---

## 1. 사전 준비

### 1.1 Kubespray 컨테이너 2.27.1로 전환

- `kubespray-contentree-1.29-1.31/kubespray.sh` 에서 이미지 버전을 **2.27.1** 로 변경 후 컨테이너 재기동.

```bash
# kubespray.sh 내
IMAGE="quay.io/kubespray/kubespray:v2.27.1"
```

- 기존 컨테이너 중지/삭제 후 다시 실행.

```bash
docker stop kubespray
docker rm kubespray
cd kubespray-contentree-1.29-1.31
./kubespray.sh
```

### 1.2 변수 파일 준비

- `inventory/variables-1.30.yaml`  
  - 1.29 → 1.30 업그레이드 시 사용.  
  - `variables-1.29.yaml` 기준으로 `kube_version` 등 1.30용으로만 수정된 파일.
- `inventory/variables-1.31.yaml`  
  - 1.30 → 1.31 업그레이드 시 사용.  
  - 1.31용 `kube_version` 등 적용.

두 파일은 미리 작성해 두고, 각 단계에서 `-e @inventory/variables-1.30.yaml` / `-e @inventory/variables-1.31.yaml` 로 지정해 사용.

---

## 2. 업그레이드 실행 순서

컨테이너 내부에서 실행 (`docker exec -it kubespray bash`).

### 2.1 1.29 → 1.30 (CP 먼저, 이후 나머지)

1. **Control Plane 먼저**

```bash
ansible-playbook -i inventory/inventory.yaml \
  -e @inventory/variables-1.30.yaml \
  upgrade-cluster.yml \
  --limit kube_control_plane
```

1. **나머지 노드(워커 등)**
  워커 실행 전 **1.3절**대로 `rook-ceph-osd` 등 ALLOWED DISRUPTIONS 0인 PDB를 삭제 또는 완화해야 drain이 성공한다.

```bash
ansible-playbook -i inventory/inventory.yaml \
  -e @inventory/variables-1.30.yaml \
  upgrade-cluster.yml \
  --limit "kube_node"
```

1. 검증

```bash
kubectl get nodes
kubectl version
```

### 2.2 1.30 → 1.31 (동일 순서)

1. **Control Plane 먼저**

```bash
ansible-playbook -i inventory/inventory.yaml \
  -e @inventory/variables-1.31.yaml \
  upgrade-cluster.yml \
  --limit kube_control_plane
```

1. **나머지 노드**
  동일하게 워커 실행 전 PDB 처리(1.3절) 적용.

```bash
ansible-playbook -i inventory/inventory.yaml \
  -e @inventory/variables-1.31.yaml \
  upgrade-cluster.yml \
  --limit "kube_node"
```

1. 검증

```bash
kubectl get nodes
kubectl version
```

---

## 3. 요약


| 단계          | 변수 파일                 | 실행 순서    |
| ----------- | --------------------- | -------- |
| 1.29 → 1.30 | `variables-1.30.yaml` | CP → 나머지 |
| 1.30 → 1.31 | `variables-1.31.yaml` | CP → 나머지 |


- Kubespray 컨테이너: **v2.27.1**  
- Playbook: `upgrade-cluster.yml`  
- 제한 실행: `--limit kube_control_plane` → `--limit "kube_node"`

